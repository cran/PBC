#######################################
####### Evaluate function given #######
#######################################

compute <- function(x, y, theta, f){
  return (eval(f))
}

margin <- function(n, x, g){
  return (eval(g))
}

